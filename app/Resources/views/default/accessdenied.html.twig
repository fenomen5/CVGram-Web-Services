{% extends 'base.html.twig' %}

{% block body %}

            <div class="alert alert-dismissible alert-danger" style="text-align: center">
                <button type="button" class="close" data-dismiss="alert">&times;</button>
                <strong>Error!</strong>
                Access denied <a class="nav-link" href="{{ path("login_page") }}">To login page</a>
                {% if link is defined and link != '' %}
                    <a class="nav-link" href="{{path(link)}}">To dashboard page</a>
                {% endif %}
            </div>

{% endblock %}
{% block stylesheets %}

{% endblock %}
